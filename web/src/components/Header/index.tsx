import { Typography } from "@heroui/react";

const Header = () => {
  return (
    <header className="container m-auto py-4">
      <Typography type="h1">Nuzlocke Tracker</Typography>
    </header>
  );
};

export default Header;
